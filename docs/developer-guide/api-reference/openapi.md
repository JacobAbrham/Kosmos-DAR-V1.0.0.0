# OpenAPI (Swagger UI)

This page embeds the live OpenAPI spec committed to the repo.

- Spec file: `docs/openapi.json` (generated from `src/main.py`)
- FastAPI Swagger UI (runtime): `GET /docs`

<swagger-ui src="openapi.json" />
